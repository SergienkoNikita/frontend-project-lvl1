export const GAME_RULES = 'Answer "yes" if the number is even, otherwise answer "no".';

export const RIGHT_ANSWERS_TO_WIN = 3;

export const ANSWER_VARIANTS = Object.freeze({
  YES: 'yes',
  NO: 'no',
});
